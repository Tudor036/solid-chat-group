import Protected from "~/components/protected";

export default function Onboarding() {
	return (
		<Protected>
			<main class="w-screen h-screen grid place-items-center px-8">
				<h1>Onboarding</h1>
			</main>
		</Protected>
	);
}
